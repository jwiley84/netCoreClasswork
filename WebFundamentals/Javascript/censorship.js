var str = "be somebody"
var arr = ["be", "bod"]


for (var strltr = 0; strltr < str.length; strltr++) { //loop through letters in string
    for (var arrwrd = 0; arrwrd < arr.length; arrwrd++) {
        if (arr[arrwrd][0] == str[i]) {
            console.log("Test one and done");
        }
    }
}